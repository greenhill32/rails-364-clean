364 Ways to Say No — Agent Review Concerns
Date: 2026-03-10
============================================


SECURITY ENGINEER FINDINGS
===========================

CAUTION — Hardcoded RevenueCat API key in git
  File: lib/revenuecat.ts line 5
  Key: appl_ieMTFqKmtaifbVskZxnUNhjZXJP
  Action: Rotate key in RevenueCat dashboard. Move to EAS Secret before re-enabling RC.

CAUTION — Apple ID + Team ID committed to git
  File: build-ios.sh lines 11-12, ExportOptions.plist
  Values: TEAM_ID=ZARBU8R8ML, APPLE_ID=greenhill2022@outlook.com
  Action: Add build-ios.sh and ExportOptions.plist to .gitignore. Move values to env vars.

CAUTION — is_purchased stored in unencrypted AsyncStorage
  File: contexts/GoldenDayContext.tsx
  Risk: Trivially bypassable on jailbroken devices.
  Action: When RC re-enabled, ensure server-side entitlement check is authoritative.
          Consider expo-secure-store (Keychain) for purchase flag.

SAFE — clearLocalCache exposed in production Settings UI
  File: components/SettingsModal.tsx
  Note: Consider removing or renaming before final release.

SAFE — Linking.openURL without error handling
  File: components/SettingsModal.tsx lines 16-22
  Note: Add try/catch. Azure Blob domain looks unprofessional — consider custom domain.

SAFE — Zero PII collected, no analytics, no ad SDKs, clean privacy posture.
SAFE — ITSAppUsesNonExemptEncryption: false correctly declared.
SAFE — Share card captures only ViewShot component, not full screen.


SENIOR DEVELOPER FINDINGS
==========================

BLOCK (CLEARED) — Privacy/Terms URLs verified live and publicly accessible. ✓

CAUTION — PaywallModal never triggered anywhere in the app
  The app is currently fully free for all users. No gating logic exists.
  Verify this is intentional for launch. PaywallModal copy says "3 free excuses used"
  which implies gating that isn't wired up.

CAUTION — react-native-purchases still live-imported in lib/revenuecat.ts
  The import is not commented out. SDK is bundled into binary even though RC is disabled.
  Action: Comment out the import in lib/revenuecat.ts.

CAUTION — getRandomFromPool() can return undefined for empty pool
  File: constants/quotes.ts (or similar)
  QuoteModal has !quote guard so no crash, but user sees a tap that does nothing.

CAUTION — AsyncStorage write inside setState updater (fire-and-forget)
  File: contexts/GoldenDayContext.tsx markDayVisited()
  React anti-pattern. Write failures are silently swallowed.

CAUTION — setGoldenDay / setPurchased typed as void but implemented as async
  Silent data loss risk if AsyncStorage write fails.

CAUTION — Linking.openURL no try/catch in SettingsModal
  Action: Wrap in try/catch, show fallback Alert on failure.

CAUTION — Hard-coded paddingTop instead of safe-area insets
  Could clip settings button or header on iPhone 14 Pro / 15 / 16 with Dynamic Island.
  Action: Replace with useSafeAreaInsets() at minimum on calendar screen.

CAUTION — as any cast on ViewShot ref in QuoteModal
  File: components/QuoteModal.tsx
  Low risk in practice due to null guard, but bypasses TypeScript.

CAUTION — Version mismatch: package.json 1.0.0 vs app.json 2.0.0
  Cosmetic only, not an App Store rejection risk.

SAFE — useWindowDimensions imported but unused in QuoteModal.tsx
  Action: Remove dead import before submission to pass lint.

SAFE — Dynamic require('react-native') inside handleShare async function
  Works correctly, just unconventional. Move Platform to top-level import.

SAFE — console.error('Share failed') left in QuoteModal
  Hermes strips console calls in release builds. No user feedback on share failure though.

SAFE — Calendar accessible without golden day set — graceful degradation, acceptable.
SAFE — wink.gif bundled asset, no load failure risk.


WHIMSY INJECTOR SUGGESTIONS (optional enhancements)
====================================================

QUICK (afternoon):
  - Replace × dismiss button with † dagger or ◆ diamond (QuoteModal)
  - Ghost watermark: large rotated diamond at 4% gold opacity behind quote (QuoteModal)
  - Month commentary pill: absurdist one-liner per month on pick screen

ONE HOUR EACH:
  - Noted shimmer: 120ms gold border flash on tap (QuoteModal)
  - Golden Day pulses once on mount, then stillness (calendar)
  - Begin button breathes: 3s slow border opacity cycle (about)
  - Splash numeral emergence: 364 fades in from darkness over 1.4s (index)

COPY + CODE:
  - Counter milestone strings: "1 excuse claimed", "10 days of dignity" etc (calendar)
  - Name the Golden Day on celebration screen with actual chosen date (golden-day-splash)
  - Past months render at 70% opacity, current month gets gold hairline rule (calendar)


PRE-BUILD v7 CHECKLIST
=======================
[ ] Rotate RevenueCat API key in RC dashboard
[ ] Comment out live import in lib/revenuecat.ts
[ ] Add build-ios.sh and ExportOptions.plist to .gitignore
[ ] Add try/catch to Linking.openURL in SettingsModal
[ ] Remove unused useWindowDimensions import from QuoteModal
[ ] Fix orange items above
[ ] Increment build number in app.json
[ ] Run npm run lint — pass clean
[ ] Build v7 via build-ios.sh
[ ] Upload to TestFlight
[ ] Test on real device
